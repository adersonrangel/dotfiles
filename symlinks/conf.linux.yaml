- clean: ['~']

- defaults:
    link:
      create: true
      force: true

- link:
    ~/.dotly: os/linux/.dotly
    ~/.gitconfig: git/.gitconfig
    ~/.homebrew: os/linux/brew/.homebrew
    ~/.config/nvim: editors/nvim
    ~/.config/herdr: editors/herdr
    ~/.config/lazygit: editors/lazygit
